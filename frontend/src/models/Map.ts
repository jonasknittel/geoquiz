export class Map {
    id: number;
    name: string;
    directory: string;

    constructor(id: number, name: string, directory: string) {
        this.id = id;
        this.name = name;
        this.directory = directory;
    }

}